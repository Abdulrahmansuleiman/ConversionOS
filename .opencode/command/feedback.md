---
description: Ask for and record feedback on the last completed task. Runs the Feedback Agent.
agent: leader
---

Run the `feedback` agent now. Give it the context it needs: summarize the most recent task that was completed (the task, which sub-agent(s) worked on it, and the QA status). Then have it ask Raymon for feedback and record it into `docs/performance/`.

Task to get feedback on (from the user): $ARGUMENTS
